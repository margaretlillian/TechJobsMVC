<h1>@ViewBag.title</h1>

@if (ViewBag.jobs != null)
{

    <h3>@ViewBag.jobs.Count Result(s)</h3>
    @foreach (Dictionary<string, string> someJobs in ViewBag.jobs)
    {
        <table class="job-listing">
            @foreach (KeyValuePair<string, string>
   job in someJobs)
            {

                <tr>
                    <td>@job.Key</td>
                    <td>@job.Value</td>
                </tr>


            }
        </table>
    }
}